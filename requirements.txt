# === Core ===
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.30.0
# pydantic>=2.11.2 requis par mistralai==2.10.0 (bump depuis 2.9.0, 2026-09-14) —
# pydantic-settings==2.5.0 reste compatible, suite de tests complète revérifiée.
pydantic==2.13.5
pydantic-settings==2.5.0

# === Base de données ===
sqlalchemy[asyncio]==2.0.32
asyncpg==0.29.0
# Repli SQLite du développement (DB_PORT injoignable hors production).
aiosqlite==0.22.1
alembic==1.13.2

# === Cache (Redis) ===
redis[hiredis]==5.0.8

# === IA / RAG ===
# Le RAG appelle directement `mistralai` (Mistral) et `qdrant-client`
# (AsyncQdrantClient) — voir app/services/rag_service.py. Le framework
# langchain n'est jamais importé ; les paquets langchain*/langsmith ont été
# retirés (2026-09-14) car inutilisés et porteurs de CVE connues.
qdrant-client==1.11.0

# === Traitement de documents ===
pypdf==6.16.1
tiktoken==0.7.0

# === IA (Mistral) : chat/vision, embeddings, Voxtral STT/TTS ===
# Migré depuis openai==1.43.0 (2026-09-14) — voir app/services/rag_service.py
# et app/services/audio_service.py. mistralai>=2.10.0 exige httpx>=0.28.1.
mistralai==2.10.0

# === HTTP Client (pour Wave API) ===
# httpx>=0.28.1 requis par mistralai (le pin <0.28 hérité d'openai==1.43.0,
# qui bloquait firebase-admin, est donc levé — non exploité pour l'instant).
httpx==0.28.1

# === Sécurité ===
# PyJWT remplace python-jose, dont les dépendances ecdsa/pyasn1 imposaient des
# versions affectées par plusieurs avis de sécurité sans correctif compatible.
PyJWT[crypto]==2.15.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
pyotp==2.9.0
pywebpush==2.5.0
# FCM API HTTP v1 (auth par compte de service) — volontairement PAS le SDK
# complet firebase-admin, dont les dépendances (httpx==0.28.1 exact) entrent
# en conflit direct avec openai==1.43.0 déjà pinné dans ce projet.
google-auth==2.58.0
pyasn1==0.6.4

# === Dev & Tests ===
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
python-dotenv==1.2.3
email-validator==2.3.0
