# === Core ===
fastapi==0.115.14
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# === Base de données ===
sqlalchemy[asyncio]==2.0.32
asyncpg==0.29.0
alembic==1.13.2

# === Cache (Redis) ===
redis[hiredis]==5.0.8

# === IA / RAG ===
# Le RAG appelle directement `openai` (AsyncOpenAI) et `qdrant-client`
# (AsyncQdrantClient) — voir app/services/rag_service.py. Le framework
# langchain n'est jamais importé ; les paquets langchain*/langsmith ont été
# retirés (2026-09-14) car inutilisés et porteurs de CVE connues.
qdrant-client==1.11.0

# === Traitement de documents ===
pypdf==6.16.1
tiktoken==0.7.0

# === Embeddings ===
openai==1.43.0

# === HTTP Client (pour Wave API) ===
httpx==0.27.0

# === Sécurité ===
# Note : pip signale un conflit pyasn1 entre python-jose (<0.5.0) et la chaîne
# google-auth (>=0.6.1) installée plus bas. Vérifié sans impact : ce projet
# n'utilise que HS256 via le backend `cryptography` de python-jose, qui
# n'exerce jamais le code dépendant de pyasn1 (réservé aux algos RSA/EC via
# le backend pur Python). Confirmé par la suite de tests complète (146/146).
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
pyotp==2.9.0
pywebpush==2.5.0
# FCM API HTTP v1 (auth par compte de service) — volontairement PAS le SDK
# complet firebase-admin, dont les dépendances (httpx==0.28.1 exact) entrent
# en conflit direct avec openai==1.43.0 déjà pinné dans ce projet.
google-auth==2.58.0

# === Dev & Tests ===
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
python-dotenv==1.2.3
email-validator==2.3.0
